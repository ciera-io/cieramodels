private service Integrity::test08_unlink_no_link_many() is
l1: instance of List;
e1: instance of Element;
e2: instance of Element;
e3: instance of Element;
l2: instance of List;
e4: instance of Element;
e5: instance of Element;
e6: instance of Element;
begin

  // Attempt to unlink a 'many' relationship that hasn't been created

  l1 := create List(name => "TODO");

  e1 := create Element(list_name => l1.name, item => "Finish videos");
  unlink e1 R1 l1;  // Attempt to unlink before linking

end service;
