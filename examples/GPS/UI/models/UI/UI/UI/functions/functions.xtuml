// BP 7.1.6 content: Package syschar: 3 persistence-version: 7.1.6

within UI::UI::UI is

  package functions is
    @dialect("oal");
    function setData(value: in real, unit: in Unit);

    @dialect("oal");
    function setIndicator(indicator: in Indicator);

    @dialect("oal");
    function setTime(time: in integer);

  end package;

end;
